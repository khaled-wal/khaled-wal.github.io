title: "Khaled Hashem | Marketing Specialist"
description: "Portfolio & Blog"
url: "https://khaled-hashem.com"
baseurl: ""

# ─── EDIT HERE: Your CV PDF path ──────────────────────────────────────────
# Upload your CV file to /assets/files/ and update the filename below
cv_path: "/assets/files/Khaled-Hashem-Resume.pdf"
# ──────────────────────────────────────────────────────────────────────────

# Collections
collections:
  case_studies:
    output: true
    permalink: /portfolio/case-studies/:name/
  projects:
    output: true
    permalink: /portfolio/projects/:name/
  lab:
    output: true
    permalink: /portfolio/lab/:name/

# Defaults
defaults:
  - scope:
      path: ""
    values:
      lang: "ar"
      dir: "rtl"
  - scope:
      path: "en"
    values:
      lang: "en"
      dir: "ltr"
  - scope:
      path: ""
      type: case_studies
    values:
      layout: case_study
      lang: "ar"
  - scope:
      path: "en"
      type: case_studies
    values:
      layout: case_study
      lang: "en"
  - scope:
      path: ""
      type: projects
    values:
      layout: project
      lang: "ar"
  - scope:
      path: "en"
      type: projects
    values:
      layout: project
      lang: "en"
  - scope:
      path: ""
      type: lab
    values:
      layout: lab_item
      lang: "ar"
  - scope:
      path: "en"
      type: lab
    values:
      layout: lab_item
      lang: "en"

# Build settings
markdown: kramdown

# Exclude development files from the _site output
exclude:
  - Gemfile
  - Gemfile.lock
  - README.md
  - "*.sh"
  - vendor/
  - .jekyll-cache/
  - .ruby-lsp/
  - node_modules/
